/*
        Instruction: implement a function named "addAttribute" the function will takes 3 arguments
        "obj" is an exist object, "key" is a attribute's name and "value" is a value of the key's property.
        Your task is add a new attribute to an exist object.

        PS: obj, key,value  will never be undefined or null

*/

export function addAttribute(obj, key, value) {
  // write your code here
}
