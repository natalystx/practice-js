/*
  Instructions: Implement a function named "addItem" that takes an array "arr" and a new item "item". 
  Your goal is to add the new item to the existing array and return the updated array.

  If arr is not defined, return null.
  If item is empty, return the original array.
*/

export function addItem(arr, item) {
  // write your code here
}
